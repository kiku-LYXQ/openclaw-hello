# Hello App Repository

本仓库的实际项目内容集中在 `hello-app/` 目录内。
如果你是第一次接触这个项目：

1. 先进入项目目录：
   ```bash
   cd hello-app
   ```
2. 阅读 `hello-app/README.md` 中的详细步骤（依赖、运行、控制、常见问题、测试）。
3. 按照指南执行命令（所有命令必须在 `hello-app` 目录下运行）：
   ```bash
   python -m snake_game
   ```

如果需要帮助或复现测试，也都在这个目录中完成。